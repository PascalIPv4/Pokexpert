# PokeExpert FR Scan Bot — Dépendances Python
# Installation : pip install -r requirements.txt

# ── Interface utilisateur ──────────────────────────
streamlit>=1.35.0

# ── Vision / OCR ──────────────────────────────────
opencv-python-headless>=4.9.0    # Traitement d'image, détection de contours
pytesseract>=0.3.10              # Wrapper Python pour Tesseract OCR
Pillow>=10.3.0                   # Manipulation d'images (PIL)
numpy>=1.26.0                    # Tableaux numpy pour OpenCV

# ── Correspondance floue ───────────────────────────
rapidfuzz>=3.9.0                 # Préféré (plus rapide que fuzzywuzzy)
# fuzzywuzzy>=0.18.0             # Alternative si rapidfuzz indisponible
# python-Levenshtein>=0.25.0     # Accélère fuzzywuzzy

# ── Web scraping ───────────────────────────────────
requests>=2.31.0                 # Requêtes HTTP
beautifulsoup4>=4.12.3           # Parsing HTML
lxml>=5.2.0                      # Parser HTML rapide (backend BS4)

# ── Utilitaires ────────────────────────────────────
python-dotenv>=1.0.0             # Variables d'environnement (.env)

# ── Système externe requis (non-pip) ──────────────
# Tesseract OCR doit être installé sur votre système :
#
# macOS    : brew install tesseract tesseract-lang
# Ubuntu   : sudo apt install tesseract-ocr tesseract-ocr-fra tesseract-ocr-eng
# Windows  : https://github.com/UB-Mannheim/tesseract/wiki
#            Ajouter le dossier Tesseract au PATH système.
#
# Vérification : tesseract --version
